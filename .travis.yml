language: python

python:
  - 3.6
  - 3.7
  - 3.8

before_install:
  - sudo apt-get install graphviz

install:
  - pip install -U pip
  - pip install tox-travis

script:
  - tox

after_success:
  - coveralls