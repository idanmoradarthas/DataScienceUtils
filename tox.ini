[tox]
envlist = py36
skipsdist = False

[testenv]
deps = -r requirements.txt
       -r requirements-dev.txt
setenv =
    PYTHONPATH = {toxinidir}
commands =
    pytest {posargs}

[coverage:run]
branch = True
source = tox_travis
parallel = True

[coverage:paths]
source =
    ds_utils